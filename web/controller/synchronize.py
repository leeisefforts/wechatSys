from application import db, appfrom common.libs.goods.goodsService import GoodsServicefrom common.libs.customer.customerService import CustomerServiceclass Synchronize:    def __init__(self):        self.goodsService = GoodsService()        self.cusService = CustomerService()    def __start__(self):        self.__synchronize__()    def __synchronize__(self):        # self.__goodsload__()        self.__cusload__()        pass    def __goodsload__(self):        pageNum = 1        data = self.goodsService.getGoodsList(1, 20)        totalCount = data.totalCount        while pageNum * 20 < totalCount:            for item in self.data:                pass        pass    def __cusload__(self):        cursor = 1        data = self.cusService.__cuslist__(cursor)        pass